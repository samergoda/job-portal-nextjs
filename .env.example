# Backend API URL (server-side only — never exposed to the browser)
API_BASE_URL=http://localhost:8080/api

# Cookie settings
AUTH_COOKIE_NAME=auth_token
AUTH_COOKIE_MAX_AGE=604800

# API versioning
API_VERSION=1.0
